<Window x:Class="SampleApplication.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:resources="clr-namespace:SampleApplication.Resources"
        Height="200"
        Width="300">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Resources/StringDictionary.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Window.Title>
        <StaticResource ResourceKey="{x:Static resources:ResourceKeys.MainWindowTitleKey}" />
    </Window.Title>

    <Button Content="{StaticResource {x:Static resources:ResourceKeys.ClickMeKey}}"
            Command="{Binding ShowResourceCommand}"/>
</Window>
 
